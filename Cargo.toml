[package]
name = "schd"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
rusqlite = { version = "0.31", features = ["bundled"] }
cron = "0.12"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
